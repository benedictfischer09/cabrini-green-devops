extends ../../layout

block content
  h2 regex
  p regex is a way of defining patterns to match text.  This technique is used in many many places in computing so there are a lot of tricks to help you create advanced patterns to match many different types of scenerios, lets learn more

  h2 Video - 13min
  p
    iframe(width="1120",height="630", src="https://www.youtube.com/embed/vcRPNhLbhoc", title="YouTube video player", frameborder="0", allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture", allowfullscreen)

  h2 Projects - 15 min
  ol
    li Write a regex to match a phone number.
    li Write a regex to match the letters a, b, or c followed by sh
    li Write a regex to match any line that starts with #

  h2 Nice Job!
  p You now know the basics of using regex!
